import { createReducer } from "redux-act";

import * as appActions from "./app.actions";

const initialState = {};

const reducer = {
  // [appActions.nameAction]: (state) => ({
  //   ...state,
  // })
};

export default createReducer(reducer, initialState);
